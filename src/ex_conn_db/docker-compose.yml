version: "3"
services:
  mariadb:
    build:
      context: mariadb/
    container_name: mariadb
    ports:
      - "3306:3306"
    networks:
      - backend

networks:
  backend:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.0.0/16
          gateway: 10.5.0.1
